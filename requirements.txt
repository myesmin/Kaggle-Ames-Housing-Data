# Ames Housing -> Real-Estate Valuation & Investment Risk Platform
# Python 3.12.  Install with:  uv venv --python 3.12 && uv pip install -r requirements.txt

# --- core scientific stack ---
numpy==2.1.3
pandas==2.2.3
pyarrow==17.0.0        # parquet engine for data/processed/
scipy==1.14.1
scikit-learn==1.5.2
statsmodels==0.14.4

# --- modelling ---
lightgbm==4.5.0
shap==0.46.0

# --- finance primitives (used in tests as the closed-form oracle) ---
numpy-financial==1.0.0

# --- plotting / geo ---
matplotlib==3.9.2
seaborn==0.13.2
folium==0.17.0

# --- data acquisition ---
requests==2.32.3
pyreadr==0.5.2          # reads ames_geo.rda (lat/long) from the AmesHousing R package

# --- notebooks + testing ---
jupyter==1.1.1
nbconvert==7.16.4
nbformat==5.10.4
ipykernel==6.29.5
pytest==8.3.3

# --- serving (see requirements-serving.txt for the container) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1           # FastAPI TestClient transport

# --- SQL feature layer ---
duckdb==1.1.3           # in-process; the warehouse is a view over data/processed/
